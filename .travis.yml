language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t jacmjoseph/docker-react -f Dockerfile .

script:
  - docker run -e CI=true jacmjoseph/docker-react npm run test -- --coverage
  